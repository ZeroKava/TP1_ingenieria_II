name: Unit Tests - SpicyTech

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: pip install pytest bcrypt PyJWT flask flask-sqlalchemy supabase

      - name: Correr tests unitarios
        run: pytest tests/unit/tests.py -v
